{
  "name": "location-tracker",
  "version": "1.0.0",
  "description": "simple location tracker based on GeoLocation API",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Artista",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
